<div class="container mx-auto px-4 py-8">
  <div class="max-w-2xl mx-auto">
    <div class="bg-white rounded-lg shadow-md p-8 text-center">
      <div class="mb-6">
        <div class="w-16 h-16 bg-red-100 rounded-full flex items-center justify-center mx-auto mb-4">
          <svg class="w-8 h-8 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
          </svg>
        </div>
        <h1 class="text-3xl font-bold text-red-600 mb-2">결제에 실패했습니다</h1>
        <p class="text-gray-600">결제 처리 중 문제가 발생했습니다.</p>
      </div>
      
      <% if @error_message || @error_code %>
        <div class="bg-red-50 border border-red-200 rounded-lg p-6 mb-6 text-left">
          <h3 class="font-semibold mb-4 text-red-800">오류 정보</h3>
          <div class="space-y-2 text-sm">
            <% if @error_code %>
              <div class="flex justify-between">
                <span class="text-gray-600">오류 코드:</span>
                <span class="font-medium text-red-600"><%= @error_code %></span>
              </div>
            <% end %>
            <% if @error_message %>
              <div class="flex justify-between">
                <span class="text-gray-600">오류 메시지:</span>
                <span class="font-medium text-red-600"><%= @error_message %></span>
              </div>
            <% end %>
            <% if @order_id %>
              <div class="flex justify-between">
                <span class="text-gray-600">주문번호:</span>
                <span class="font-medium"><%= @order_id %></span>
              </div>
            <% end %>
          </div>
        </div>
      <% end %>
      
      <div class="space-y-3">
        <%= link_to "다시 결제하기", payments_checkout_path, class: "block w-full bg-blue-600 text-white py-3 px-4 rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 font-semibold" %>
        <%= link_to "장바구니로 돌아가기", cart_path, class: "block w-full bg-gray-200 text-gray-800 py-3 px-4 rounded-md hover:bg-gray-300 focus:outline-none focus:ring-2 focus:ring-gray-500 font-semibold" %>
        <%= link_to "쇼핑 계속하기", root_path, class: "block w-full bg-gray-100 text-gray-600 py-3 px-4 rounded-md hover:bg-gray-200 focus:outline-none focus:ring-2 focus:ring-gray-400 font-semibold" %>
      </div>
      
      <div class="mt-6 pt-6 border-t border-gray-200">
        <p class="text-sm text-gray-500">
          문제가 지속되면 고객센터로 문의해 주세요.<br>
          전화: 1588-1234 | 이메일: support@example.com
        </p>
      </div>
    </div>
  </div>
</div>
